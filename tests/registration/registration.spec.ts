import { test } from '@playwright/test';

test.describe('Sign Up', () => {
    test.fixme('should be able to sign up with discord', async ({page}) => {});
    test.fixme('should be able to sign up with google', async ({page}) => {});
    test.fixme('should be able to sign up with regular email/password', async ({page}) => {});
});