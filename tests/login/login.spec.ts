import { test } from '@playwright/test';


// test.describe.configure({ mode: 'serial' });
// let page: Page;
// test.beforeAll(async ({ browser }) => {
//   page = await browser.newPage();
//   await page.goto(urls.home.url);
// });
// test.afterAll(async () => {
//   await page.close();
// });


test.describe('Log In', () => {
    test.fixme('should be able to login with discord', async ({page}) => {});
    test.fixme('should be able to login with google', async ({page}) => {});
    test.fixme('should be able to login with regular email/password', async ({page}) => {});




});